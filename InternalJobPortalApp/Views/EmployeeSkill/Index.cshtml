@model IEnumerable<EmployeeSkillLibrary.EmployeeSkill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmpId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkillId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkillExp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmpId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkillId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkillExp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { empId = item.EmpId, skillId = item.SkillId }) |
            @Html.ActionLink("Details", "Details", new { empId = item.EmpId, skillId = item.SkillId }) |
            @Html.ActionLink("Delete", "Delete", new { empId = item.EmpId, skillId = item.SkillId }) |
            @Html.ActionLink("GetByEmployeeId", "GetByEmployeeId", new { empId = item.EmpId}) |
            @Html.ActionLink("GetBySkillId", "GetBySkillId", new { skillId = item.SkillId })
        </td>
    </tr>
}

</table>
